# Живой тест поставщиков на «Волге», 26 сентября 2026

Запрос пользователя: проверить опубликованный сценарий на тендере
`0171200001926000664`. Исходная версия AutoBot `a511b0e`, checkout `75bcaf9`.
Исходный diff: ранее изменённый `data/search_resume_checkpoint.json` и
неотслеживаемая `.impeccable/`; они не относятся к проверке.

Наблюдаемый результат: из настоящего интерфейса CRM выбрать шесть связанных
позиций, выполнить поиск, получить проверяемые компании/контакты/цены и
сгруппированные обращения. Режим `draft`; реальные отправки в этом тесте
не запускаются. Сохранность сметы и старого outbox сверяется по SHA-256.
Это реальный подбор по рабочим позициям, без синтетических записей в рабочей БД.

Выбраны работы 25/26/27 по прокладке кабеля (6,95 / 3,45 / 3,45 × 100 м),
материалы 64 (сжим У733М, 1,72 × 100 шт), 67 (муфта 4ПКТп(б)-1-16/25(Б),
86 шт), 71 (кабель АВБбШв 4х150, 351,9 пм). Подтверждённые цены других
позиций не затрагиваются. Первый run: `252430212053485da2935c88f7371ae7`.
Сформированы 7 запросов, включая профиль подрядчика и Авито, и 66 проверок
источников. До запуска outbox: blocked 1 / sent 2 / uncertain 2.

Найден дефект: единица веса «кг» в названии монтажной работы распознаётся как
марка кабеля КГ. Решение: исключить из cable_model только числовые значения
веса, сохранив настоящие названия «Кабель КГ 3х2,5». Проверка должна доказать
отсутствие ложной марки в итоговом обращении и сохранение действительных
ограничений марки/массы. Изменение паспорта не переписывает старые обращения;
после публикации нужен новый подбор по актуальному снимку.

План проверки: изолированные tests market requirements/source adapters/buyer,
самопроверка diff; дождаться первого живого подбора, оценить качество результатов,
проверить группировку, объёмы, контакты, фото и источник цены; после исправления
проверить актуальный результат и повторное открытие. Полный suite и отправка
писем не входят в этот тест. Результаты дополняются по наблюдаемому поведению.

Первый проход завершился `partial`: 43 страницы проверены, 23 недоступны или
не подтверждают ассортимент; 36 компаний/черновиков, одна опубликованная цена
41,85 ₽/шт для У733М. Это предварительный результат до исправления качества.
9 запросов Авито вернули HTTP 429; ограничения не обходились. Охват обращениями
не означает подтверждённые цены или доступность товара.

Дополнительно обнаружены адреса поддержки справочников, email оператора
персональных данных вместо продавца и закодированный HostCMS email.
Исправления: 8 наблюдаемых доменов справочников/площадок исключены из выбора
поставщиков и повторной проверки старого адреса; служебные контексты и HTML
комментарии не дают получателя; собственный email сайта имеет приоритет;
явный ROT13 mailto декодируется без выполнения JavaScript; переход контактной
страницы на другой сайт не примешивает чужой адрес. Обычные почтовые сервисы
и явно опубликованный email торгового юрлица на другом домене сохраняются.
Ссылки на контакт внутри той же страницы не загружаются повторно.

Изолированные проверки после основных исправлений: 147 buyer/hermes unittest
с предварительным импортом настоящего web_ui; 139 pytest по требованиям,
источникам, кабелю, работам и многокатегорийным регрессиям — PASS.
Самопроверка diff выполнена на потерю ограничений, ложных адресатов, настоящую
марку КГ, актуальность источника и отсутствие отправок.

Первый пакет опубликован как `98a00da`: в Linux-кандидате 147 unittest и
проверка числового «кг» — PASS. Перед заменой контейнера сохранены четыре
SQLite с проверкой восстановления, три отчёта и предыдущий Docker-образ.
После публикации совпали хеши 13 таблиц и трёх отчётов, health OK,
неавторизованные jobs/report/image возвращают 401. CRM не перезапускалась.
Backup: `/opt/crm-backups/volga-buyer-20260926T142652Z`.

Повторный run `d68ccae8972644199bdb1f95426a6752` остановлен: живая выдача
показала статьи о майнинге и компьютерах. Причины: резервный поисковик
игнорирует `site:avito.ru`, а совпадения категории искались по всей странице,
включая меню/подвал. На остановке 19 кандидатов; подготовка обращений не
выполнялась. Outbox (5 записей) и три XLSX совпали с исходными хешами.

Второй пакет: ограничение домена `site:` проверяется самим ботом; профиль
поставщика подтверждается заголовком/основным содержимым и коммерческим
предложением; статьи/инструкции исключены. Точные товарные запросы используют
написанные в смете модель и размер вместо длинного сметного описания;
проверка источника требует эти идентификаторы. Снимок получает версию правил:
старые результаты остаются в истории, но отправка их обращений блокируется,
повторный запуск создаёт актуальный подбор. Источники и цены не подменяются.

Второй пакет: 152 buyer/hermes unittest (настоящий web_ui, сеть отключена),
139 pytest по требованиям/источникам/многокатегорийным регрессиям — PASS.
Проверены ошибочная модель товара, статья со словами из подвала, инструкция
вместо подрядчика, чужие домены в выдаче Авито и блокировка старой политики.
Публикация второго пакета и итоговый живой прогон ещё проверяются.
