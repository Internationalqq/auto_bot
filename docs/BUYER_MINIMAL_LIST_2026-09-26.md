# Минимальный список отправок

Исходный AutoBot `13cfd29`, production UI `fd5dc8d`; CRM `741466b`.
Изменение checkpoint и untracked-файлы сохранены. Этап — уточнение существующего
интерфейса по выбранной пользователем таблице, без изменений почтового скрипта.

Наблюдаемый результат: три колонки «Кому / Отправлено / Ответ», фактический
адресат, прошедшее время и короткий статус. Подробности письма и позиций
доступны в раскрытии строки. Повторяющиеся ошибки проверки заменяются общей
заметкой; отсутствие сохранённого ответа при ошибке не называется ожиданием.

Время считается только от подтверждённой отправки, обновляется независимо от
изменений API, без пересоздания строки и потери фокуса. Точная дата доступна
в подсказке. Неопределённые отправки сохраняют свой статус, время создания
не выдаётся за время отправки. Сохраняются цены, фильтры, поиск, отправка,
журнал, ограничения повторов и разделение переписки по фактическому адресату.

Проверка: поведенческие frontend-наборы (время, статусы, обновление без
перерисовки), браузер на копии данных 390/768/1280, раскрытие ответа,
длинные адреса и фильтры. Самопроверка diff, резерв и проверка production.
Миграции данных и новые письма не входят в этот пакет.

Реализовано: убраны изображения, состав сметы, повторы статуса отправки,
количества обращений и фрагменты ответов из свёрнутой строки. Текст ответа,
состав запроса, журнал и действия остаются внутри. Цены с оговорками сохранены.
Время обновляется каждые 15 секунд и при возвращении на вкладку, включая
неизменный ответ API и ошибку соединения; письмо проверяется прежним скриптом.

Проверено до выпуска:

- 3 frontend-набора: buyer, buyer_correspondence, tender_workspace — PASS.
  Новые проверки: границы минут/часов/дней, сдвиг часов, отсутствие времени,
  uncertain без выдуманной отправки, повторный render без пересоздания строки,
  обновление при недоступном API и возврате на вкладку. Старые проверки
  фактического адресата, разделения общей рассылки, буквального HTML, цен и
  фильтров сохранены и адаптированы к краткому виду строки.
- 23 изолированных pytest теста tender_detail_filters / tender_next_action —
  PASS, внешние соединения запрещены, рабочие базы не используются.
- Копия данных Волги в IAB: 390/768/1280, горизонтального переполнения нет;
  7 отправленных строк по 68–71 px на компьютере. Проверены Enter, видимый
  фокус, сохранение фильтра/раскрытия после обновления, длинный ответ 2380
  символов (62 в имени строки), пустой список и ошибка загрузки.
- Самопроверка diff: доступы/API/деньги/серверная логика не менялись; никаких
  новых отправок или повторов. Самопроверка не является независимым ревью.
  Полные наборы репозитория не запускались. Известная ошибка Mail.ru reader
  не исправлялась; общая заметка не позволяет принять её за отсутствие ответа.

Опубликован `ee8058e79d80bba309db7090e5ef93fab7d25a47`, assets `20260926-6`.
Linux-кандидат из git archive прошёл 175 buyer/hermes unittest без сети и
рабочих данных. Перед переключением: отправок/проверок 0, до проверки 80 с.
Sender кратко остановлен и восстановлен в finally, launchd running. CRM
runtime не перезапускалась, почтовые модули совпали по SHA с прежним кодом.

Резерв: `/opt/crm-backups/volga-buyer-20260926T181409Z`, штатная проверка
восстановления SQLite прошла. Rollback: `crm-autobot:rollback-buyer-ui-ee8058e`.
После переключения неизменны 13 таблиц и 3 отчёта Волги, hashes всех UI-файлов
совпали с исходниками, health OK, закрытые API без авторизации — 401.
Manifest: `/opt/crm-releases/buyer-ui-ee8058e79d80bba309db7090e5ef93fab7d25a47/verified.json`.

В авторизованном production-браузере проверены новые assets, три колонки,
7 отправленных строк, 1 сохранённый ответ, 2 неподтверждённые отправки во
фильтре «Все». Реальный ответ Ярстройтех76 раскрывается одним нажатием.
Ширина iframe 680 px, scrollWidth 680 px. Снимок и локальные проверки:
CRM `outputs/buyer-correspondence-20260926/minimal-live.png`, `minimal-layout.json`.
Пакет завершён. Ошибка поиска входящих Mail.ru остаётся отдельной задачей.
