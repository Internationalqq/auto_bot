# Отправка заявки скриптом в Firefox, 26 сентября

Исходное состояние: AutoBot ff110e1, runtime feb12c5; CRM 90ca0a8.
Сохранены посторонние изменения data/search_resume_checkpoint.json и .impeccable.

Результат пакета: один реальный запрос по Волге, проверка в отправленных и
подключение этого способа к обычной очереди CRM без вызова модели для отправки.
Существующие адресаты/uncertain, история попыток, роли и цены сметы сохраняются.

Выбран ООО ЯМК: https://yamk76.ru/shop/inertnye-materialy/shheben/shheben-frakcziya-20-40/
Публичный контакт stal-snab@inbox.ru и адрес в Ярославле проверены на сайте.
Цена на странице без подтверждённых единицы/М1200 не принята как сопоставимая.
Заявка fe3846644c6e458c91f10f0d9eb14411: М1200 20–40, 57,859 м³,
собственная цена поставщика, НДС, наличие и доставка в Ярославскую область.

Первоначальный autobot-mail run_b80e24d32d4843579de97279e10766df завершился
Connection error после capture, без клика отправки. AX-only проверка тоже
получила обрыв соединения. Причина связи не установлена.

Современная форма Mail.ru в Firefox не фиксировала ввод адресата; все проверки
поймали это до Send. Собственный испорченный черновик отменён. Через штатную
ссылку доступной версии открыта light.mail.ru с тем же входом. Обычные HTML-поля
корректно заполняются AXValue: отдельно проверены реальные значения, пустые
копии и полное тело, затем screenshot. Пароли/cookies не переносились,
защита драйвера не отключалась, новые агенты не создавались.

Реальная отправка выполнена скриптом, без AI. Mail.ru показал «Письмо отправлено»;
письмо открыто из «Отправленные» по совпадению строки получателя и темы.
Message URL: https://light.mail.ru/message/17904192240858699569/?folder=500000
Полный текст и адресат сверены. Ответ поставщика пока не получен.

Механизм: sender_mode=mailru_lite_script, один autobot-mail desktop lock.
После подготовки fsync-журнал записывается ДО Send. После потери подтверждения
повтор запуска только сверяет отправленные, никогда повторно не нажимает Send.
Сверка страницы проверяет sent folder, отправителя, адресата и полное тело.
Проверка списка пока ограничена первой страницей отправленных; основная защита
повторов — серверный outbox и сохранённый журнал, сомнительные старые попытки
не переотправляются. Пагинация/поиск старых писем — следующий отдельный пакет.

Добавлен защищённый reconcile_sent для владельца исходной lease: допускает
только финализированный uncertain → sent с точной прежней квитанцией и новым
доказательством. Сохраняет прежний результат в существующую таблицу истории;
схема БД и остальные записи не изменяются. Это не сброс и не разрешение повторов.

Проверки: 100 Python-тестов PASS в изолированной копии (9 script, 7 single,
29 hermes, 9 jobs, 22 workflow, 18 outbox, 6 campaigns); buyer_frontend_tests.js
PASS. Промежуточный pattern test_buyer_replies.py не нашёл тестов и не включён
в счётчик; ответы проверяются существующим workflow-набором. Полный suite не
запускался. Макет не менялся, адаптивные размеры заново не проверялись.

На Mac FirefoxLight и затем полный script.execute с зарезервированной попыткой
нашли собственное отправленное письмо без нового Send. Первый read-back тела
опередил загрузку страницы; добавлены до трёх повторов только чтения. Финальная
проверка прошла. Скриншот и sent-proof.json сохранены в script-pilot каталоге
исходной попытки 5be302ffd75f5613ca695a79.

Опубликован runtime 60b8197646f103d3c1070b9cce01140fa69404b5. Linux candidate:
110 тестов PASS (те же 100 и 10 queue), health/auth smoke PASS. Резерв
/opt/crm-backups/buyer-send-20260926T104957Z содержит прежний image и SQLite.
CRM image, nginx и отчёты Волги сохранены. Server outbox переведён uncertain→sent
через reconcile_sent с проверкой всех полей доказательства; повтор сверки
не создал вторую запись. Остальные outbox-строки побайтово по значениям сохранены.
Старая uncertain-квитанция доступна в истории, sent-proof.json лежит в
data/buyer-pilot-evidence/fe3846644c6e458c91f10f0d9eb14411-sent-proof.json,
SHA256 350b0bb53f40bba2437d9bfb729fdcad6e317df46c939db39c46688ffb05dc00.

Mac sender_mode=mailru_lite_script установлен, buyer-sender перезапущен штатно.
Резерв /Users/egor/.hermes/backups/mail-script-20260926T104904Z содержит прежний
sender.json и код. Новых AI-профилей нет. Шаблоны также готовятся без модели;
существующая проверка ответов пока остаётся через единственный autobot-mail
и не доказана реальным ответом нового поставщика. Объёмы поиска сайтов и
процент покрытия этой отправкой не увеличивались искусственно.

Откат: вернуть сохранённые sender.json и buyer_sender.py при остановленном
buyer-sender, затем штатно bootstrap его plist; сервер — image из manifest
резерва, docker compose up только autobot. Не восстанавливать старую SQLite
поверх актуальной: иначе потеряется факт отправки. Следующий шаг — скриптовый
поиск/чтение ответов и пагинация отправленных, расширение поиска контактов по
непокрытым категориям. Реальную цену принимать только из ответа или сопоставимой
карточки поставщика, с единицей/НДС/доставкой.
