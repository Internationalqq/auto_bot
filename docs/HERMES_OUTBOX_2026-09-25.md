# Короткие запросы и отправка с Mac

Исходное состояние AutoBot `74dfe42`, CRM `27a2008`. Чужой diff
`data/search_resume_checkpoint.json` и `.impeccable/` сохраняются.

Пользователь разрешил исправить тексты и отправлять запросы поставщикам.
Подтвердил маршрут: CRM → Mac → действующий агент с открытыми аккаунтами.
Рабочий почтовый профиль `zakup_alibert`, Mail.ru `pm.build.team@mail.ru`.
Пароли и cookies не копируются; существующий SMTP-helper не используется.

## Решение и проверяемый результат

Составитель получает только название, характеристики, объём/единицу, регион
и адрес доставки. Внутренний ID, номер тендера, разделы, цены, бюджет,
накладные и резерв остаются в исходном журнале. Версия снимка 2 отделяет
новые тексты от старых; старые задания сохраняются, отправка их заблокирована.
Проверка результата отклоняет внутренние оговорки и числовые цены в рублях.
Она не заменяет проверку смысла пользователем.

Готовый текст → один email → «Отправить через Mac». Отдельная SQLite-очередь
`buyer_outbox.sqlite3`, отдельный исходящий Mac-процесс `buyer_sender`.
Действующий Hermes-профиль получает точный текст и одного адресата, использует
Mail.ru в уже открытом Firefox и проверяет «Отправленные». Без вложений,
покупок, звонков и смены аккаунтов. Статус `sent` означает подтверждение
отправленных агентом, не доставку и не полученную цену.

Перед запуском сохраняется локальная попытка. При потере связи сервер
не передаёт письмо другому исполнителю; повторный клик возвращает ту же
запись. Перезапуск восстанавливает квитанцию или `uncertain`, не повторяет
отправку. `blocked` / `uncertain` требуют разбора, автоповтора нет.
После устранения причины для `blocked` доступен явный повтор. Предыдущая
квитанция сохраняется в новой таблице `outbound_attempt_history`, новый
lease-токен отсекает старого исполнителя; локальные попытки имеют разные
подкаталоги. Для `sent` и `uncertain` повтор запрещён. Существующие записи
не преобразуются; таблица истории создаётся отдельно в БД исходящих.
Квитанция требует совпадения задания/получателя и локального файла доказательства;
в CRM возвращается только имя/хеш, без cookies и логов агента.

## Выпуск и данные

Новая изолированная БД создаётся штатно; преобразований смет/цен/CRM нет.
До выпуска — SQLite backup очереди черновиков, rollback-образ AutoBot,
фиксация образа CRM, nginx и хешей отчётов «Волги», проверка отсутствия
активной обработки. Кандидат проверяется отдельно, затем переключается
только AutoBot. Mac: резерв кода и отдельный LaunchAgent отправки;
существующие профили не перенастраиваются. Откат образа и остановка только
`ai.autobot.buyer-sender`; сохранить outbox/journals для сверки отправленных.
Старый Mac poller черновиков перезапускается после проверки, что не работает.

## Проверки до выпуска

На изолированной копии 29 тестов Hermes, 9 очереди/worker/API, 8 outbox,
10 agent_market_queue PASS; tender_review — 9 pytest PASS. Старый unittest
запуск tender_review нашёл 0 тестов; выполнен правильный pytest-прогон.
Frontend buyer scope PASS. Полный suite не запускался.
Живой Hermes: щебень М1200 20–40 мм, 57,859 м³ → короткий запрос своей цены,
НДС, наличия/срока и возможности доставки; вопросов о накладных нет.
UI на копии: 390/768/1280, нет горизонтального переполнения, новая отправка
и повтор → одна запись, старый текст не отправляется. Внешняя отправка
тестовому адресу не выполнялась: эта очередь изолирована от Mac.

Выпуск сервера `b29c066`, резерв `/opt/crm-backups/buyer-send-20260925T182323Z`.
45 целевых Linux-тестов прошли. Candidate auth сначала имел неуказанный
PMBI_CRM_URL и вернул ожидаемый конфигурационный 503 вместо 401; с адресом
CRM health и оба запрета доступа прошли. CRM-образ, nginx и отчёты сохранены.
Mac-only исправление сохранения попытки `0c1f73d`: fsync перед запуском;
сбой после старта классифицируется uncertain, не blocked.

На production обновлены 3 обращения. Первая отправка из браузера CRM на
публичный адрес `yarstroyteh76@mail.ru` (источник https://yarstroyteh76.ru/)
создала `de08671e042e44798d15d14dd22ebf25`. Агент вернул blocked без
действий в Firefox: launchd PATH не включал ~/.local/bin, где установлен
cua-driver. Исправлен только новый LaunchAgent; doctor подтверждает
cua-driver 0.28.2, Accessibility и Screen Recording. Первый запуск Hermes
автоматически установил свой Node/agent-browser/Chromium до обнаружения
неполного PATH; настройки действующих профилей не менялись. Повтор выполняется
как отдельная явная попытка с сохранением этой квитанции.
Следующая попытка CLI также blocked: служебный процесс ожидал подтверждения
в несуществующем терминале и получил timeout. Отправка переведена на штатный
локальный API отдельного профиля `autobot-mail`, 127.0.0.1:8645. Разрешены
только computer_use/file, память и cron отключены, параллельность 1. Обычная
политика подтверждений сохранена; YOLO/обход подтверждений не включались.
Рабочая почта остаётся в Firefox на Mac; логин на ПК не требуется.

API-клиент проверяет профиль и список инструментов. Сохранённый run_id после
перезапуска опрашивается без нового POST. Неоднозначный старт запрещает повтор;
ранее созданные CLI-журналы распознаются и не перезапускаются. 11 outbox-тестов
PASS (вместо 8); повторно также 29 Hermes, 9 buyer_jobs, 10 market_queue и
frontend buyer scope PASS. Полный suite не запускался.

Первая API-попытка остановилась до составления письма: у Firefox несколько
окон, Hermes передавал window_id при AX-клике, но терял его при координатном
клике. В установленном Mac-адаптере сохранены прежние локальные изменения;
узкая исправляющая утилита `scripts/fix_hermes_pointer_window.py` добавляет
window_id из последнего снимка к click/drag/scroll и отказывает без выбранного
окна. Полный исходник сохранён в `cua_backend.py.autobot-pointer-20260925T184813Z`.
Новый тест с 5 вариантами действий и 17 существующих keyboard/window recovery
тестов на Mac PASS. Перезапущен только новый API отправителя. Политика
подтверждений, права macOS и другие работающие агенты не менялись.

Последующий запуск `run_ea4cb92a6e6d4516b7390cc4e7e0ee6c` также blocked до
отправки: cua-driver отказал во вводе с `off_space_or_ax_unresolved`, а
focus_app не подтвердил доступность окна. Промежуточный blocked был вызван
посторонней вкладкой с окном входа; инструкция уточнена — переходить в Mail.ru,
не авторизуясь на другом сайте и не выдавая дополнительных разрешений.
Всего у пилотной записи пять попыток, ни одна не дошла до отправки.
Пользователю запрошено открыть рабочий Firefox/Mail.ru на текущем рабочем
столе самого Mac. Это не просьба входить в почту на ПК. После подтверждения
доступности — только явный повтор существующей blocked-записи из CRM.

Опубликован `21b539daf784e8fd8672e052e331c56a5f3e960f`; резерв
`/opt/crm-backups/buyer-send-20260925T185312Z`. В Linux-кандидате 49 тестов
Hermes/buyer_jobs/outbox PASS, health и запреты доступа без авторизации PASS.
После выпуска проверены revision, health, неизменность CRM-образа, nginx
и хешей отчётов. В авторизованной production-CRM подтверждён статус blocked,
текст запроса и кнопка явного повтора. Это ещё не успешная внешняя отправка.
Пока не реализованы автоматический подбор всех получателей, входящие ответы,
импорт цены из КП, Авито/MAX/WhatsApp и голос в новом маршруте CRM.
