[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "auto_bot"
version = "0.0.0"
description = "Парсинг zakupki, сметы, Алиса, merge, веб-отчёт"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["autobot*"]
